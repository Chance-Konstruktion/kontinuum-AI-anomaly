# Was die Testsuite braucht -- und sonst nichts.
#
# kontinuum-core steht hier, weil die Tests dagegen laufen und nicht
# dagegen tun als ob. Das Paket liegt auf PyPI; die Quelle dazu unter
# https://gitlab.schanz.ipv64.net/chance-konstruktion/kontinuum-core
#
#     pip install -r requirements_test.txt
#     pytest

pytest
pytest-asyncio
kontinuum-core
